SEO Index Checker Dashboard
Панель керування на базі Streamlit для масової перевірки індексації посилань у Google. Інструмент дозволяє організовувати посилання за проектами та папками, перевіряти їх статус через DataForSEO API, зберігати результати в Supabase та автоматично надсилати звіти у Slack.

🛠 Технічний стек
Core: Python 3.x, Streamlit

Database: Supabase (PostgreSQL)

API Provider: DataForSEO (Google Organic SERP, endpoint task_post / task_get)

Notifications: Slack SDK (WebClient)

Data Processing: Pandas, OpenPyXL, Requests

⚡ Функціонал
Організація даних: Ієрархічна структура «Проекти ➝ Папки» для сегментації посилань.

Масовий імпорт: Зручне додавання списків URL (copy-paste) із пакетним записом у БД.

Логіка перевірки:

Використання пошукового оператора site:domain/path.

Асинхронна черга завдань (Batch requests по 50 URL).

Автоматичне визначення статусу (Indexed / Not Indexed).

Звітність та інтеграції:

Генерація Excel-звіту (.xlsx) із результатами.

Автоматичне відправлення файлу звіту у вказаний канал Slack.

UI/UX:

Двомовний інтерфейс (Українська / Англійська).

Індикатори прогресу перевірки.

Механізм Retry для стабільності з'єднання з базою даних.

🚀 Встановлення та налаштування
1. Клонування та залежності
Bash

git clone <repository_url>
cd <repository_folder>
pip install -r requirements.txt
Примітка: Якщо файлу requirements.txt немає, встановіть основні бібліотеки вручну:

Bash

pip install streamlit supabase pandas openpyxl requests slack_sdk
2. Конфігурація середовища (Secrets)
Створіть файл .streamlit/secrets.toml у корені проекту та додайте ключі доступу. Структура файлу повинна точно відповідати наведеній нижче:

Ini, TOML

[supabase]
url = "YOUR_SUPABASE_URL"
key = "YOUR_SUPABASE_ANON_KEY"

[dataforseo]
login = "YOUR_DFS_LOGIN"
password = "YOUR_DFS_PASSWORD"
host = "api.dataforseo.com"

[slack]
bot_token = "xoxb-YOUR-SLACK-BOT-TOKEN"
channel_id = "YOUR_CHANNEL_ID"
3. Налаштування Бази Даних (Supabase)
Виконайте наступний SQL-код у SQL Editor вашого проекту Supabase для створення необхідних таблиць та зв'язків:

SQL

-- 1. Таблиця проектів
create table projects (
  id bigint generated by default as identity primary key,
  name text not null,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- 2. Таблиця папок
create table folders (
  id bigint generated by default as identity primary key,
  project_id bigint references projects(id) on delete cascade,
  name text not null,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

-- 3. Таблиця посилань
create table links (
  id bigint generated by default as identity primary key,
  project_id bigint references projects(id) on delete cascade,
  folder_id bigint references folders(id) on delete cascade,
  url text not null,
  status text default 'pending', -- можливі статуси: pending, done, error
  is_indexed boolean,
  task_id text,
  last_check timestamp with time zone,
  created_at timestamp with time zone default timezone('utc'::text, now())
);
